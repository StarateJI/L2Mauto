# 🗺️ L2Mauto — Roadmap

Документ ведётся с 2026-09-14. Здесь: что сделано, что в работе, что в планах.
Кураторы: пользователь (владелец) + ИИ-ассистент (Z.ai Code).

---

## 📊 Текущий статус (на 2026-09-15, v5.6.16)

### Что работает стабильно ✅
- **Аукцион (Аукцион)** — прогон 20 окон за 17.4 мин (≈52 сек/окно)
  - Выход из энерго через цикл 3× `turn_off` с проверкой `is_on()` между
  - Поиск предмета: matchTemplate → красная точка как подтверждение
  - Анти-вечный-цикл (статус «Продаётся» через OCR)
  - Пустой список = `'empty'` (не `'error'` — не стопает прогон)
  - `_ensure_foreground` (AttachThreadInput трюк) — клики в правильное окно
- **Главный GUI** — переименование кнопок на русский:
  Аукцион, Данжи, Химка, ПВП, Бонусы, Шедуля, СТОП
- **Чистая консоль** — фильтр важной инфы (whitelist/blacklist)
- **Логи пачек** — старт/конец + длительность + финальный отчёт «Прогон за X мин»
- **Логи в GitHub** — авто-загрузка логов+PNG в ветку `bot-logs`
- **Автообновление** — multi-source (raw + API + jsdelivr, берёт MAX)
- **Бонусы (Rewards)** — закрытие рекламных баннеров в `_open_menu`

### Что в активной работе 🔄
- **Поиск предмета Lupi** — matchTemplate находит B&W-дубликат с score 0.92,
  но красная точка не ловится радиусом 40px. Реальный предмет с красной
  точкой в другой ячейке. Нужно: либо увеличить радиус, либо искать по
  другому принципу (цветная иконка vs B&W)
- **Выход из сна 100%** — текущий цикл 3× работает (20/20 окон в тесте),
  но если `is_on()` сам врёт (false negative) — фикс не поможет.
  Запасной вариант: проверка яркости окна (спящее = тёмное)

### Что в планах 📋
- **Перекрытие окон** при >1 окна в пачке — пока оставлено 1 окно в пачке
- **Выбор кол-ва окон в пачке** пользователем (сейчас hardcoded)
- **Сетка выбора окон для Шедули** — при запуске Шедули вылезает окно со всеми окнами (сетка), пользователь выбирает/убирает какие будут в прогоне. Сетка запоминает выбор с прошлого прогона (все выбраны по умолчанию, кликаешь чтобы убрать, в следующий раз уже убранные)
- **Возврат к пропущенным окнам** — код есть, но не тестировался
- **«Недо-энерго»** в конце прогона (энерго не всегда включается с 1-й попытки)
- **Нейронки под каждый профиль** (см. раздел «Долгосрочные планы» ниже)
- **Google Sheets интеграция — учёт алмазов** (долгосрочное)
  - Пользователь ведёт Google-таблицу: ник окна → кол-во алмазов
  - Настроить service account → дать доступ к таблице → JSON-ключ в `secrets/`
  - Этап 1 (проще): после успешной продажи бот добавляет цену к текущему
    количеству алмазов в таблице (`sheets_writer.py`, `update_diamonds(win, delta)`)
  - Этап 2 (точнее): бот распознаёт алмазы с экрана (OCR верхний правый угол)
    и синхронизирует таблицу с актуальным числом — убирает накопление ошибки
  - Вызовы: в `auction._one_item_cycle` после «введена цена N» →
    `update_diamonds(window_id, price)`

---

## 📋 История версии 5.6.x (фиксы аукциона)

| Версия | Фикс |
|--------|------|
| 5.6.7 | Поиск предмета: matchTemplate первичен, красная точка как ПОДТВЕРЖДЕНИЕ |
| 5.6.8 | Анти-вечный-цикл: проверка статуса «Продаётся» |
| 5.6.9 | `_ensure_foreground` (AttachThreadInput трюк) + OCR мусор не «Продаётся» |
| 5.6.10 | ❌ Сломанные тайминги внутри пачки (REVERTED в 5.6.11) |
| 5.6.11 | REVERT 5.6.10 — `wait_f` и `_wait_auction_loaded` возвращены как были |
| 5.6.12 | Пустой список = `'empty'` (не `'error'`) — не стопает прогон |
| 5.6.13 | Чистая консоль (whitelist/blacklist фильтр) + логи пачек |
| 5.6.14 | Всегда `turn_off(ignore=True)` — фикс Снегопад |
| 5.6.15 | Убран спам про обнову из консоли |
| 5.6.16 | Цикл 3× `turn_off` с проверкой `is_on()` — фикс Zakamsk (логи врали) |

---

## ❌ Известные баги (на 2026-09-15)

| Баг | Статус | Приоритет |
|-----|--------|-----------|
| Lupi: поиск не находит предмет с красной точкой | 🔄 в работе | высокий |
| `is_on()` false negative на спящем окне | ⚠️ может сломать фикс 5.6.16 | средний |
| Перекрытие при >1 окна в пачке | 📋 отложено (1 окно в пачке) | низкий |
| «Недо-энерго» в конце прогона | 📋 отложено | низкий |

---

## 📊 Текущий статус (на 2026-09-14, v5.0.6) — архив

### Что работает стабильно ✅
- **Главный GUI** (PyQt5 + qasync) — все кнопки профилей на месте
- **TG-бот** (aiogram) — управление с телефона, уведомления, скриншоты
- **Перехват ввода** (interception-драйвер) — клава+мышь, человекоподобные движения
- **Захват экрана** (Rust `capture_rs.pyd`) — DXGI/WGC, mss как fallback
- **Распознавание цифр алхимии** (PyTorch CNN, 7 классов) — `bot/alchemy/main_alch.py`
- **Окна:** автоподгон 400×225, память позиций безымянных окон
- **Расписания:** 15-минутное окно + догон пропущенного
- **Автообновление** со своего GitHub-репо через `apply_update.bat`
- **Логи** — `RotatingFileHandler` 10 МБ × 5 файлов, цветной stdout
- **`log_uploader`** — авто-загрузка логов+PNG в ветку `bot-logs` (finally блок)

### Что в активной работе 🔄
- **Аукцион: перестановка лотов** (`bot/methods/game/auction.py`)
  - Снятие лота ✅ (ОК с 1-й попытки на 1280×720)
  - Поиск предмета в инвентаре ⚠️ (переписан на matchTemplate, тестируется)
  - Ввод цены минус 1 ⚠️ (OCR через Tesseract, ввод калькулятором)
  - Возврат окна на место ✅ (после 5.0.6 — даже при падении)

### Что не работает / требует фикса ❌
- См. раздел «Известные баги» ниже

---

## ✅ Что уже сделано (история фиксов)

### Стабильность
- ✅ Честные ошибки запуска — тихая смерть окон побеждена
- ✅ Пачки окон не зависают на проблемных окнах (try/except в `start_seq`)
- ✅ Живучие расписания (окно 15 мин + догон пропущенного)
- ✅ `BotManager._graceful_stop` — ровно один раз, без гонок
- ✅ `_run_bot` — логирует полный traceback при падении бота

### Умные механики
- ✅ Память окон — опознание безымянных по позиции (`windows_memory.py`)
- ✅ Автоподгон размера к 400×225 без смены позиции (`_try_autofix`, 3 попытки)
- ✅ Умное засыпание окон с подтверждением
- ✅ Авто-раскладка окон сеткой (`gui/tiling.py`)

### Шедуля-режим
- ✅ Закуп через игровое расписание — «запустил и спи»
- ✅ Камера-часовой Шедули с авто-перезапуском
- ✅ Перезапуск по окончании 8-часового цикла — круглосуточно

### Аукцион
- ✅ Читалка цен через Tesseract OCR (`auction_eye.py`)
- ✅ Снятие лота: клик Отмена → окно подтверждения → ОК (4 попытки)
- ✅ Resize 1280×720 на (100,100) — обходит CEF-баг 2560×1440
- ✅ Авто-возврат окна на исходную позицию (даже при падении)
- ✅ `notify_screenshot` при ошибке — слать скрин в TG
- 🔄 Поиск предмета (переписан на multi-scale matchTemplate, тестируется)
- 🔄 Ввод цены калькулятором (логика есть, нужен тест)

### GUI / обновления
- ✅ Система обновы со своего репо + кнопка в GUI
- ✅ `apply_update.bat` — копирование после закрытия Python (фикс Permission denied на .pyd)
- ✅ Проверка обновы раз в 60 сек (было 3600)
- ✅ `needs_update` логирует результат и ошибки (не молчит)
- ✅ `tg.ini [github] token` — для приватных репо и log_uploader

### Логи и диагностика
- ✅ `log_uploader` — авто-загрузка логов+PNG в ветку `bot-logs`
- ✅ `try/finally` в `reregister()` — логи уходят даже при падении бота
- ✅ CRASH REPORT header в логе при падении (error, window, made)

---

## ❌ Известные баги (надо фиксить, не критично)

| # | Баг | Где | Симптом | Приоритет |
|---|---|---|---|---|
| 1 | `memory allocation of 14745616 bytes failed` | `capture_rs.cp311-win_amd64.pyd` | Падение на 1 ПК при аукционе (фрейм 2560×1440×4) | HIGH |
| 2 | TG polling падает на `ClientConnectorError` | `tgbot/bot.py` | Если нет интернета/прокси к api.telegram.org | MED |
| 3 | Жадные префиксы в callback_query | `tgbot/handlers/manage.py` `start_` / `stop_` | Кнопка «stop_all» ловится как «stop window all» | LOW |
| 4 | Ник с `_` ломает `start_nick_profile` | `tgbot/handlers/manage.py:801` | `start_Dark_Lord_Farm` → ник Dark, профиль Lord_Farm | LOW |
| 5 | Висячие песочные часы на кнопках | Несколько handlers | Нет `callback.answer()` в ветке ошибки | LOW |
| 6 | Дубликат «🏥 Главное меню:» в 4 файлах | menu/manage/global_menu/notifications | При смене текста — 4 правки | LOW |
| 7 | `ignore.py` — пустой файл-заглушка | `tgbot/handlers/ignore.py` | Можно наполнить фичей | LOW |
| 8 | Превью лога в TG ломается на спецсимволах | `tgbot/handlers/logs.py` | HTML-инъекция через `<` `>` `&` | LOW |
| 9 | Лог в TG обрезается на 1024 символа | `tgbot/handlers/logs.py` | Превью > 8 строк → отказ отправки | LOW |

---

## 🚀 План развития (Roadmap)

### 🎯 Ближайшие задачи (1-2 недели)

#### Аукцион — доделать «relist» (минус 1 от минималки)
- [ ] Тест 5.0.6: проверка `matchTemplate` на реальных скринах
- [ ] Если TM не сработает — попробовать `cv2.matchTemplate` с маской (HSV)
- [ ] Если и это не сработает — попробовать образец из confirm-window (`CONFIRM_ITEM`)
- [ ] Финальный тест полного цикла: снять → найти → клик → OCR цена → ввести → ОК → Добавить
- [ ] Поставить аукцион на расписание (раз в день/час)

#### Стабильность
- [ ] Фикс memory allocation на проблемном ПК (вариант: перейти на mss для аукциона)
- [ ] Проверить 64-битность Python (`python -c "import struct; print(struct.calcsize('P')*8)"`)
- [ ] Увеличить pagefile до 16+ ГБ на проблемном ПК
- [ ] Логировать RSS памяти бота каждые 5 минут (для диагностики утечек)

#### Мелкие баги (можно批量 пофиксить за один заход)
- [ ] Вынести «🏥 Главное меню:» в одну константу в `tgbot/keyboards/menu.py`
- [ ] Починить разбор кнопок с `_` в нике (использовать `split('_', 2)` вместо `split('_')`)
- [ ] Добавить `callback.answer()` во все ветки ошибок (убрать песочные часы)
- [ ] HTML-экранирование превью лога (`html.escape()`)
- [ ] Обрезать превью лога до 800 символов

---

### 🧠 Большая задача: нейронки под каждый профиль

**Цель:** для каждого профиля найти фреймворк/модель, которая будет работать лучше, чем сейчас (распознавание по пикселям и CBT-триггерам).

Сейчас в боте:
- **Алхимия** — уже на PyTorch CNN (`bot/alchemy/main_alch.py`, класс `DigitCNN`, 7 классов цифр)
- **Аукцион** — Tesseract OCR для цен, переходим на matchTemplate для иконок
- **Другие профили** (PvP, Buyer, Rewards, Dungeon, Scheduler) — пиксель-чекинг через `check_pixel`

**Проблема:** пиксель-чекинг хрупкий. Изменилась тема игры — сломалось. Изменился размер окна — сломалось. Малейший сдвиг — `check_pixel` промахивается.

**План — по профилям:**

#### 1. Алхимия (уже на CNN, улучшить)
- [ ] Расширить классы: добавить цифру `0` (сейчас 1,3,5,6,7,8,9 — не хватает 0,2,4)
- [ ] Обучить на большем датасете (собрать 1000+ примеров через `au_sample.png`-подобный сборщик)
- [ ] Перевести CNN на ONNX-runtime (быстрее, меньше зависимостей)
- **Фреймворк-кандидат:** `torchvision` (уже стоит) → `onnxruntime` для инференса
- **Альтернатива:** YOLOv8 для детекции слотов (если CNN будет ошибаться на сложных фонах)

#### 2. Аукцион: детекция лотов
- **Сейчас:** matchTemplate (фикс от 5.0.6)
- **Проблема:** если поставить 2 разных предмета — найдёт только первый
- **Цель:** детекция ВСЕХ лотов + их идентификация
- **Фреймворк-кандидат:** **YOLOv8** (`ultralytics`)
  - Обучить на ~100-200 скринах с разметкой (иконки предметов)
  - Классы: «сапоги», «шлем», «оружие», «броня», «аксессуар»
  - Инференс на CPU: ~50ms на страницу
- **Альтернатива:** VLM через API (GLM-Vision, Claude Vision) — отправлять скрин, получать список предметов. Минус: задержка 2-5 сек, нужен интернет.

#### 3. PvP Dodge
- **Сейчас:** `check_pixel` на триггер «2 меча»
- **Проблема:** если меняется UI (патч игры) — сломается
- **Цель:** распознавать PvP-ситуацию по всей сцене, а не по 1 пикселю
- **Фреймворк-кандидат:** **YOLOv8** для детекции вражеских меток/красного контура
- **Альтернатива:** `imagehash` (perceptual hash) — сравнение с эталоном «PvP-сцена»

#### 4. Buyer (закупка)
- **Сейчас:** клик по фиксированным координатам NPC
- **Проблема:** если NPC сдвинулся (новый патч) — клик мимо
- **Цель:** найти NPC на экране, кликнуть по нему
- **Фреймворк-кандидат:** **YOLOv8** для детекции NPC по цвету/форме
- **Альтернатива:** `cv2.matchTemplate` с шаблонами NPC (если их мало)

#### 5. Rewards (сбор наград)
- **Сейчас:** пиксель-чекинг на «жёлтую точку» (непрочитанная награда)
- **Проблема:** если меняется цвет/форма метки
- **Цель:** детекция «есть непрочитанная награда» по нескольким признакам
- **Фреймворк-кандидат:** простая **CV-логика** (HSV-фильтр на жёлтый + морфология) — без нейронки
- **Если не хватит:** YOLOv8 для детекции иконки почты/награды

#### 6. Dungeon (данжи)
- **Сейчас:** клик по таймингам
- **Проблема:** если меняется расписание или UI входа в данж
- **Цель:** распознавать «окно входа в данж открыто» по структуре окна
- **Фреймворк-кандидат:** `cv2.matchTemplate` на иконку входа (она стабильная)
- **Альтернатива:** простая CV-логика (проверка нескольких пикселей)

#### 7. Scheduler (шедуля)
- **Сейчас:** клик по CBT-кнопкам расписания
- **Проблема:** если меняется UI
- **Цель:** детекция «расписание активно» по структуре окна
- **Фреймворк-кандидат:** `cv2.matchTemplate` на иконку статуса

---

### 📚 Сводная таблица фреймворков

| Профиль | Текущий метод | Целевой метод | Фреймворк | Сложность |
|---|---|---|---|---|
| Алхимия | PyTorch CNN (digit) | ONNX-runtime + больше классов | `onnxruntime` | Низкая |
| Аукцион | matchTemplate | YOLOv8 (детекция всех лотов) | `ultralytics` | Высокая |
| PvP Dodge | check_pixel | YOLOv8 (детекция вражеских меток) | `ultralytics` | Высокая |
| Buyer | фиксированные координаты | YOLOv8 (детекция NPC) | `ultralytics` | Средняя |
| Rewards | check_pixel | CV-логика (HSV + морфология) | `opencv` | Низкая |
| Dungeon | клик по таймингам | matchTemplate (иконка входа) | `opencv` | Низкая |
| Scheduler | CBT-кнопки | matchTemplate (иконка статуса) | `opencv` | Низкая |

---

### 🎯 Долгосрочные задачи (1-3 месяца)

#### Нейросеть для аукциона (приоритет 1)
- [ ] Собрать датасет: ~200 скринов аукциона с разными лотами
- [ ] Разметить через `labelme` или Roboflow
- [ ] Обучить YOLOv8n (nano) —最快, ~50ms на CPU
- [ ] Классы: ~10-15 типов предметов (сапоги, шлемы, мечи, щиты, кольца, etc.)
- [ ] Экспорт в ONNX, интеграция в `bot/methods/game/auction.py`
- [ ] Заменить `matchTemplate` на YOLO-predict

#### VLM-интеграция (приоритет 2, для сложных сцен)
- [ ] При критической ситуации бот шлёт скрин в GLM-Vision/Claude через API
- [ ] Модель описывает сцену текстом → бот парсит → действие
- [ ] Применение: «бот застрял» → VLM смотрит → «ты в воде, нажми кнопку游泳»
- [ ] Минус: задержка 2-5 сек, нужен интернет, токены

#### Унификация CV-пайплайна (приоритет 3)
- [ ] Создать `bot/vision/` модуль
- [ ] Базовый класс `Detector` с методами `detect(image) -> List[BBox]`
- [ ] Наследники: `YOLODetector`, `TemplateMatcher`, `PixelChecker`
- [ ] Профили используют `Detector` вместо `check_pixel`
- [ ] Логирование всех детекций (для отладки)

#### Web-дашборд (приоритет 4, опционально)
- [ ] Flask/FastAPI сервер на 8080 порту
- [ ] Дашборд: список окон, статусы, графики (фарм/час, ошибки/час)
- [ ] Live-лог в браузере (WebSocket)
- [ ] Скрины по клику (как в TG, но в браузере)

---

## 📋 Правила работы над проектом

### Регламент (из старой переписки, работает)
1. **Одна правка = один прогон = один доклад**
2. **Файл целиком**, не кусками (только если файл > 1000 строк)
3. **Каждая просьба — с точной командой**
4. **Каждая правка — с именем файла**
5. **Готовый проверенный инструмент — ПЕРЕД самоделкой** (использовать `cv2.matchTemplate` вместо своего SIFT-костыля)
6. **КАЖДЫЕ ~10 СООБЩЕНИЙ — перечитывать регламент**

### Коммуникация
- Я (ИИ) коммичу в `main` через GitHub PAT
- Бот обновляется через кнопку «♿️ Доступна обнова!» (раз в 60 сек)
- Логи+PNG автоматически уходят в ветку `bot-logs`
- Я читаю логи через `git fetch` + VLM для PNG
- Пользователь не копипастит код — только нажимает кнопку обновы

### Версионирование
- `5.0.x` — фиксы багов
- `5.1.x` — новые фичи (когда добавим нейронки)
- `5.2.x` — рефакторинг (унификация CV-пайплайна)
- `6.0.0` — мажорный релиз (веб-дашборд, мульти-оконный фарм с очередью)

---

## 📞 Контакты / ссылки

- **Репозиторий:** https://github.com/StarateJI/L2Mauto
- **Ветка логов:** `bot-logs` (логи + debug PNG)
- **Версия:** `5.0.6` (см. `bot/version.txt`)
- **TG-бот:** настроить через `tg.ini [bot] token`
- **GitHub PAT:** в `tg.ini [github] token`

---

## 📝 История версий

| Версия | Дата | Что сделано |
|---|---|---|
| 5.0.0 | 2026-09-13 | Свой апдейтер + все фиксы (форк от L2Monad) |
| 5.0.1 | 2026-09-13 | Тест кнопки обновы |
| 5.0.2 | 2026-09-14 | Resize fix (сохранение позиции окна), log_uploader, 60s update check |
| 5.0.3 | 2026-09-14 | updater: timeout 2→10s, читает токен из tg.ini, логирует ошибки |
| 5.0.4 | 2026-09-14 | Плавные свайпы (21 точка вместо 2), SIFT threshold 4→3, cluster 30→50 |
| 5.0.5 | 2026-09-14 | apply_update.bat — фикс Permission denied на залоченных .pyd |
| 5.0.6 | 2026-09-14 | LOT_SEARCH fix (75,190,80,70), multi-scale matchTemplate, try/finally |

---

_Документ обновляется по мере развития проекта. Последнее обновление: 2026-09-14._
